import React from 'react';
import {MoviesInfo} from "../../components/MovieInfo/MoviesInfo/MoviesInfo";

const MovieInfoPage = () => {
    return (
        <div>
            <MoviesInfo/>
        </div>
    );
};

export {MovieInfoPage};